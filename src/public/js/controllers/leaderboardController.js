app.controller('leaderboardController', ['$scope', '$http', '$location', 'currentUser', function($scope, $http, $location, currentUser) {

    // $scope.view = {};
    //
    // $http.get('users/' + currentUser.id + '/games/' + game.game_id).then(function(game) {
    //     var gameDetails = game.data;
    //     console.log(gameDetails);
    // })

}]);
